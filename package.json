{
    "name": "bun-cdk",
    "module": "index.ts",
    "type": "module",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "format": "prettier --write .",
        "lint": "eslint .",
        "dist": "bun scripts/build-all.ts",
        "compose": "bun scripts/compose-up.ts"
    },
    "workspaces": [
        "apps/*",
        "common/*"
    ],
    "devDependencies": {
        "@types/bun": "latest",
        "eslint": "^9.12.0",
        "prettier": "^3.3.3",
        "typescript-eslint": "^8.9.0"
    },
    "peerDependencies": {
        "typescript": "^5.0.0"
    }
}
